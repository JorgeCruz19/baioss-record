# Dirección de la API del Record. Vacío = mismo origen (el proxy de Vite la reenvía a http://127.0.0.1:5005).
# Solo hace falta cambiarla si el cliente se sirve desde otro sitio y la API se expone con CORS.
VITE_API_BASE_URL=
