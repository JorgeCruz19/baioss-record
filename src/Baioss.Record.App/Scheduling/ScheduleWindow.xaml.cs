using System.Windows;

namespace Baioss.Record.App.Scheduling;

/// <summary>Gestor de grabaciones programadas (lista + alta). El DataContext es un ScheduleViewModel.</summary>
public partial class ScheduleWindow : Window
{
    public ScheduleWindow() => InitializeComponent();
}
